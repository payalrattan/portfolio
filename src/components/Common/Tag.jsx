const Tag = ({ label }) => {
  return (
    <span className="inline-block bg-blue-50 text-xs font-semibold text-[#2563eb] rounded-full px-2 py-1 mr-2 mb-2">
      {label}
    </span>
  );
};

export default Tag;
